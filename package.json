{
  "name": "chaverola",
  "version": "0.0.0",
  "private": true,
  "description": "Chaverola — a fun, game-like multiplayer classroom chat activity.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter client dev",
    "build": "pnpm --filter client build",
    "preview": "pnpm --filter client preview",
    "typecheck": "pnpm --filter client typecheck",
    "lint": "pnpm --filter client lint",
    "test": "pnpm --filter client test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.8.0"
  }
}
